/**
 * Chess to Lichess — Chess.com Content Script
 * Previously contained the Floating Action Button (FAB).
 * FAB has been removed in v1.4.0 — transfer is now triggered via
 * the extension icon (popup or one-click mode) or keyboard shortcut.
 */
